# LMS.Assessment.Api

An API for managing law firm details.

## Setup

- Run API
  ```
  dotnet restore
  dotnet tool restore
  dotnet run
  ```

- Run tests

  `dotnet test`

- Run mutation tests

  `dotnet stryker`
